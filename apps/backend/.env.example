PORT=3010
DB_HOST=localhost
DB_PORT=5432
DB_USER=escronet
DB_PASSWORD=escronet
DB_NAME=escronet

# Firebase — path to the service account JSON downloaded from Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_SERVICE_ACCOUNT_JSON=/path/to/firebase-service-account.json
