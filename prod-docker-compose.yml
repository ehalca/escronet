services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: escronet
      POSTGRES_DB: escronet
      # POSTGRES_PASSWORD is read from prod.env
    volumes:
      - ./postgres:/var/lib/postgresql/data
    env_file:
      - ./prod.env

  backend:
    image: ehalca/escronet:backend
    restart: unless-stopped
    depends_on:
      - postgres
    environment:
      PORT: 3010
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: escronet
      DB_NAME: escronet
      NODE_ENV: production
    volumes:
      - ./firebase-service-account.json:/run/secrets/firebase-service-account.json:ro
    env_file:
      - ./prod.env
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  frontend:
    image: ehalca/escronet:frontend
    restart: unless-stopped
    environment:
      PORT: 3011
      NODE_ENV: production
    env_file:
      - ./prod.env
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt:ro
    depends_on:
      - backend
      - frontend

  certbot:
    image: certbot/certbot
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    entrypoint: >
      sh -c "trap exit TERM;
             while :; do
               certbot renew --webroot -w /var/www/certbot --quiet;
               sleep 12h & wait $${!};
             done"

  watchtower:
    image: containrrr/watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 30 --label-enable
    environment:
      WATCHTOWER_CLEANUP: "true"
      DOCKER_API_VERSION: "1.45"
